# EntHub 工程操作规则
#
# 本文件会被进入本项目的 AI 会话自动加载。记录的是踩过坑后确立的硬性纪律。

# 1. 数据文件只读，绝不在原地操作
#    - /Users/gm/AI/名单 和 /Users/gm/Downloads 下的 Excel/CSV 是用户的原始数据，
#      只允许读取，禁止 rename / move / delete / 覆盖写入。
#    - 凡需对原始文件做处理，先复制到临时目录（tempfile.gettempdir()）的副本上操作。
#    - 2026-07 事故：测试导入预演时直接把原始 Excel 路径喂给 _import_worker，
#      预演结束的 _cleanup 用 os.remove 删掉了原始文件。已加 _is_temp_safe 白名单兜底，
#      但根本纪律是「测试只碰副本」。

# 2. 导入流程相关
#    - import_flow.py 已是流式版本（openpyxl read_only），不要再退回 pandas 全量加载。
#    - 预演 / 测试 worker 时，DB_PATH 一定指向 /tmp 下的副本库，绝不指向 data/enthub.db。
#    - 喂给 worker 的 meta.files[].path 必须是临时副本路径，不能是用户原始路径。
#    - _cleanup 只删临时目录内的文件（_is_temp_safe 白名单），不要为了"清理"放宽这个限制。

# 3. 空值永远不覆盖已有数据
#    - UPDATE 只写「传入值非空且与库里不同」的字段。空字段 = 文件没带，不等于要清空。
#    - 这是 import_flow 的核心规则，改动去重/diff 逻辑时要守住。

# 4. 被质疑时先查证，别急着下结论
#    - 用户问"是不是你动的文件"时，先查时间戳、命令记录、代码逻辑，再回答。
#    - 不要凭"我代码里没写某逻辑"就断言自己没干，也不要为迎合用户而做不准确的承认。
#    - 承认错误要准确：是删除就说删除，是移动就说移动，不要混用措辞。

# 5. 改动前先看现状
#    - 动任何代码前先读文件、理解现有模式，不臆测结构。
#    - 用户已存在的未提交改动不回退、不覆盖，除非用户明确要求。
