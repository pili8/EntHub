# EntHub 多端演进路线图

> 本文档总结多端访问与手机离线方案的讨论结论，作为后续演进的决策依据。
> 创建时间：2026-07-18 ｜ 当前版本：**v0.2.0**

---

## 一、当前架构现状

```
┌─────────────────────────────────┐
│  Flask 后端 (app.py)            │
│  + SQLite 数据库 (enthub.db)    │
│  + Jinja2 模板（服务端渲染）     │
└─────────────────────────────────┘
              │
              ├─→ Web 浏览器（Mac 本地）  ✅ 已可用
              └─→ /api/* JSON 接口（仅 3 个）⚠️ 不完整
```

### 核心约束

- 后端用 **Python Flask** 写
- Python 在手机端几乎无法运行（iOS 不允许后台服务；Android 通过 Termux 体验差）
- 因此「一套 Flask 代码打包到手机」这条路 **走不通**

---

## 二、思源笔记的对照参考

思源能做到「全平台离线可用」，根本原因：

| 维度 | 思源笔记 | EntHub |
|------|---------|--------|
| 后端语言 | Go（可交叉编译到 Android/iOS） | Python（无法手机运行） |
| 数据存储 | 本地文件系统 + SQLite | SQLite |
| 多端架构 | 一套后端 + 多端壳 | 暂只有 Mac 壳 |

**结论**：EntHub 不能直接复刻思源路线，必须分阶段演进。

---

## 三、手机离线方案对比（按改动量排序）

| 方案 | 思路 | 离线 | 改动量 | 适合场景 |
|------|------|------|--------|----------|
| **A. 只读离线 + PWA** | Flask 导出离线包，手机 PWA 加载查询 | ✅ 只读 | ⭐ 1-2 天 | 手机以查看为主 |
| **B. PWA + sql.js** | 浏览器跑 SQLite WASM，数据库文件同步到手机 | ✅ 读写 | ⭐⭐⭐ 3-5 天 | 查询复杂、编辑少 |
| **C. Capacitor 原生壳** | Web 前端套壳成 App，本地 SQLite 插件 | ✅ 读写 | ⭐⭐⭐ 5-7 天 | 想要"真 App"体验 |
| **D. cr-sqlite 多端同步** | 各端 SQLite + CRDT 自动合并 | ✅ 读写 | ⭐⭐⭐⭐ 5-10 天 | 多端编辑需求 |
| **E. Go 重写后端** | 走思源路线，跨平台交叉编译 | ✅ 读写 | ⭐⭐⭐⭐⭐ 30+ 天 | 推倒重来 |

---

## 四、分阶段演进策略

### 🅰️ 阶段一：当前版本完善（v0.3 ~ v1.0）

**目标**：在 Flask 架构下把「多端在线访问」和「开放 API」做扎实，榨干当前技术栈的价值。

#### 任务清单（按优先级）

| # | 任务 | 目的 | 预计版本 |
|---|------|------|----------|
| 1 | 补齐 REST API（企业/详情/关联/统计） | 让其他应用可调用 | v0.3 |
| 2 | MCP Server 封装 | Claude/Cursor 等 AI 工具可调 | v0.3 |
| 3 | Docker 部署 | 脱离 Mac，部署到 NAS/云服务器 | v0.4 |
| 4 | 监听地址改 `0.0.0.0` | 局域网设备可访问 | v0.4 |
| 5 | PWA 改造（manifest + service worker） | 手机浏览器加桌面图标，接近原生 | v0.5 |
| 6 | API Key 鉴权 | 开放 API 后的安全性 | v0.6 |

#### 阶段一不做的

- ❌ 手机离线
- ❌ 重写后端
- ❌ 复杂的同步机制

### 🅱️ 阶段二：从底层重构（v2.0）

**目标**：实现真正意义上的全平台离线可用。

**触发条件**（满足任一即可考虑启动）：
- 阶段一所有任务已完成并稳定运行 3 个月以上
- 手机离线成为真实业务刚需（而非「想要的」）
- Tauri Mobile / cr-sqlite 等关键技术成熟度提升

**候选技术栈**（到时再讨论定）：

| 层 | 候选 | 备注 |
|----|------|------|
| 后端语言 | Go / Rust | 都可交叉编译到 Android/iOS |
| 桌面壳 | Tauri / Wails | 替代当前 EntHub.app |
| 移动壳 | Tauri Mobile / Capacitor | 取决于后端选型 |
| 数据同步 | cr-sqlite | CRDT 自动合并，最优雅 |
| 前端 | 保留 HTML 或换 React/Vue | 看后端选型 |

---

## 五、阶段一第一步：补齐 REST API

这是当前版本「对其他应用开放」的核心，也是未来重构时能复用的资产——**接口定义不变，只换底层实现**。

### 建议补的端点

| 端点 | 功能 |
|------|------|
| `GET /api/companies` | 企业列表（支持筛选、分页） |
| `GET /api/companies/<id>` | 企业详情（含关联记录） |
| `GET /api/search` | 统一搜索（已存在，需完善） |
| `GET /api/relations` | 关联查询（电话/邮箱/法人/股东） |
| `GET /api/stats/legal_person` | 法人统计 |
| `GET /api/stats/shareholder` | 股东统计 |
| `GET /api/stats/industry` | 行业统计 |
| `GET /api/tags` | 标签管理（已存在） |

---

## 六、决策记录

- ✅ **当前阶段**：在 Flask 版本上完善多端访问能力（在线模式）
- ✅ **未来阶段**：从底层重构支持手机离线
- ❌ **不采纳**：现在就重写（项目刚稳定，价值未榨干）
- ❌ **不采纳**：方案 E（Go 重写），除非转为开源产品

---

## 七、参考与备注

- **思源笔记架构**：https://github.com/siyuan-note/siyuan
- **cr-sqlite**：https://github.com/vlcn-io/cr-sqlite
- **sql.js**：https://github.com/sql-js/sql.js
- **Capacitor**：https://capacitorjs.com/
- **Tauri Mobile**：https://v2.tauri.app/

---

*本文档为讨论结论记录，具体实施时仍以代码确认和测试为准。*
